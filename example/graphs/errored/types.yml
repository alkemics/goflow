name: Errors

inputs:
  - a
  - b

nodes:
  - id: int_add
    type: nodes.Add
    bind:
      a: inputs.a
      b: inputs.b

  - id: uint_add
    type: nodes.UAdd
    bind:
      a: inputs.a
      b: inputs.b
